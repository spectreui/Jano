import Wrapper from "../layout/wrapper";
import WebHosting from "./home/web-hosting";

const MainRoot = () => {
  return (
    <Wrapper>
      <WebHosting />
    </Wrapper>
  );
};

export default MainRoot;
